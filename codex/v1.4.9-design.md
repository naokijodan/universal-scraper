# v1.4.9 設計書 — 全プラットフォーム段落統一 + 失敗/要確認 UI 改善

## 背景

- 椛島さんから 2 点の改修要望:
  - (A) 他プラットフォームは商品説明を 1段落化しているが、ヤフオク等が不統一
  - (B) v1.4.8 後、サイドパネルで「失敗をクリア」ボタンが「機能してない」ように見える
- 3者協議 (Claude + GPT-5 + Gemini) で方針確定:
  - 段落統一は「改行除去 + パイプ区切り」
  - 失敗/要確認の UI セマンティクスを分離

## ゴール（5 項目）

| # | 内容 | 重要度 |
|---|---|---|
| 1 | 全プラットフォーム説明文段落統一（パイプ区切り） | HIGH |
| 2 | スタットカード分割（失敗 N / 要確認 M） | HIGH |
| 3 | クリアボタン動的 disabled | MEDIUM |
| 4 | clearFailed の早期 alert 文言改善 | MEDIUM |
| 5 | retryFailed の alert 文言改善（v1.4.8 拡張） | LOW |

合計約 55 行。バージョン v1.4.8 → v1.4.9。

---

## 1. 全プラットフォーム説明文段落統一

### 統一テンプレート
```javascript
description = description
  .replace(/\r\n/g, ' ').replace(/\r/g, ' ').replace(/\n/g, ' ')
  .replace(/ /g, ' ').replace(/ /g, ' ')
  .replace(/\t/g, ' ')
  .replace(/\s+/g, ' ')
  .trim();
```

### 修正対象（4 サイト、content.js）

#### 1-A. yahuoku (line 6071 周辺)
カテゴリ/コンディション/ブランドを `\n` で追記している箇所を ` | ` に変更し、最後に統一テンプレート適用。

```diff
 let description = itemJson.description || '';
 if (itemJson.category && itemJson.category.path) {
-  description += `\nカテゴリ: ${categoryPath}`;
+  description += ` | カテゴリ: ${categoryPath}`;
 }
 if (itemJson.conditionName) {
-  description += `\nコンディション: ${itemJson.conditionName}`;
+  description += ` | コンディション: ${itemJson.conditionName}`;
 }
 if (itemJson.brand && itemJson.brand.path) {
-  description += `\nブランド: ${brandPath}`;
+  description += ` | ブランド: ${brandPath}`;
 }
-description = description.replace(/\t/g, '  ');
+// §18 (v1.4.9) 全プラットフォーム段落統一
+description = description
+  .replace(/\r\n/g, ' ').replace(/\r/g, ' ').replace(/\n/g, ' ')
+  .replace(/ /g, ' ').replace(/ /g, ' ')
+  .replace(/\t/g, ' ')
+  .replace(/\s+/g, ' ')
+  .trim();
```

#### 1-B. rakuten (line 4083 周辺)
`.join('\n')` を ` | ` に変更 + 統一テンプレート。

```diff
 text = text.split('\n')
   .filter(line => { /* フィルタ */ })
-  .join('\n');
+  .join(' | ');
+// §18 (v1.4.9) 全プラットフォーム段落統一
+text = text
+  .replace(/\r\n/g, ' ').replace(/\r/g, ' ').replace(/\n/g, ' ')
+  .replace(/ /g, ' ').replace(/ /g, ' ')
+  .replace(/\t/g, ' ')
+  .replace(/\s+/g, ' ')
+  .trim();
```

#### 1-C. yahooshopping (line 8465, 8522, 8593)
3 箇所の `.join('\n')` を `.join(' | ')` に変更 + 各箇所で統一テンプレート適用。

```diff
-productInfoText = infoLines.join('\n');
+// §18 (v1.4.9) 全プラットフォーム段落統一
+productInfoText = infoLines.join(' | ')
+  .replace(/\r\n/g, ' ').replace(/\r/g, ' ').replace(/\n/g, ' ')
+  .replace(/ /g, ' ').replace(/ /g, ' ')
+  .replace(/\t/g, ' ')
+  .replace(/\s+/g, ' ')
+  .trim();
```

同じパターンを line 8522 / 8593 にも適用。

#### 1-D. hardoff (line 7155)
`.join('\n')` を `.join(' | ')` に変更 + 統一テンプレート。

```diff
-const description = descriptionLines.join('\n');
+// §18 (v1.4.9) 全プラットフォーム段落統一
+const description = descriptionLines.join(' | ')
+  .replace(/\r\n/g, ' ').replace(/\r/g, ' ').replace(/\n/g, ' ')
+  .replace(/ /g, ' ').replace(/ /g, ' ')
+  .replace(/\t/g, ' ')
+  .replace(/\s+/g, ' ')
+  .trim();
```

### 変更しないファイル
- mercari / mercari_shop / rakuma / paypayfurima / amazon / ebay は既に 1段落化（または最終的に空白置換）されているので変更不要

---

## 2. スタットカード分割（失敗 / 要確認）

### options.html — カード追加

既存の「失敗」カード (line 689 周辺) の後ろに「要確認」カードを追加:

```diff
           <div class="queue-stat-item queue-stat-failed" id="queue-stat-failed-card" role="button" tabindex="0" title="クリックで失敗のみ表示／全件表示を切り替え">
             <span class="queue-stat-label">失敗</span>
             <span class="queue-stat-value" id="queue-stat-failed">0</span>
           </div>
+          <!-- §18 (v1.4.9) 要確認カード（unknown_remote_state のみ） -->
+          <div class="queue-stat-item queue-stat-unknown">
+            <span class="queue-stat-label">要確認</span>
+            <span class="queue-stat-value" id="queue-stat-unknown">0</span>
+          </div>
```

CSS 追加:
```css
    /* §18 (v1.4.9) 要確認カード */
    .queue-stat-item.queue-stat-unknown .queue-stat-value { color: #ef6c00; }
```

### options-queue.js — computeStats を拡張

`computeStats` (line 121 周辺) で `failed` を「通常 failed」と「unknown」に分離:

```diff
   function computeStats(queue) {
     const now = getNow();
-    let waiting = 0, sending = 0, sent = 0, failed = 0, retrying = 0;
+    let waiting = 0, sending = 0, sent = 0, failed = 0, unknown = 0, retrying = 0;
     for (const q of (Array.isArray(queue) ? queue : [])) {
       if (!q || typeof q !== 'object') continue;
       if (q.status === 'sending') sending++;
       else if (q.status === 'sent') sent++;
-      else if (q.status === 'failed') failed++;
+      else if (q.status === 'failed') {
+        // §18 (v1.4.9) unknown_remote_state は failed と別カウント
+        if (q.lastError === 'unknown_remote_state') unknown++;
+        else failed++;
+      }
       else if (q.status === 'waiting') {
         if ((q.nextRetryAt || 0) > now) retrying++;
         else waiting++;
       }
     }
-    return { waiting, sending, sent, failed, retrying };
+    return { waiting, sending, sent, failed, unknown, retrying };
   }
```

### renderStats を拡張

```diff
   function renderStats() {
     const s = computeStats(state.queue);
     const el = {
       waiting: $('queue-stat-waiting'),
       sending: $('queue-stat-sending'),
       retry: $('queue-stat-retry'),
       sent: $('queue-stat-sent'),
-      failed: $('queue-stat-failed')
+      failed: $('queue-stat-failed'),
+      unknown: $('queue-stat-unknown')
     };
     if (el.waiting) el.waiting.textContent = String(s.waiting);
     if (el.sending) el.sending.textContent = String(s.sending);
     if (el.retry) el.retry.textContent = String(s.retrying);
     if (el.sent) el.sent.textContent = String(s.sent);
     if (el.failed) el.failed.textContent = String(s.failed);
+    if (el.unknown) el.unknown.textContent = String(s.unknown);
```

---

## 3. クリアボタン動的 disabled

### options-queue.js — renderStats 内に追加

通常 failed が 0 件のときボタンを disabled に:

```diff
     if (el.unknown) el.unknown.textContent = String(s.unknown);

+    // §18 (v1.4.9) 通常失敗が 0 件のときクリアボタンを無効化
+    const clearBtn = $('queue-clear-failed-btn');
+    if (clearBtn) {
+      clearBtn.disabled = (s.failed === 0);
+      clearBtn.title = (s.failed === 0 && s.unknown > 0)
+        ? '通常失敗はありません。「要確認」' + s.unknown + ' 件は個別対応してください'
+        : '';
+    }
```

---

## 4. clearFailed 早期 alert 文言改善

### options-queue.js — clearFailed (line 453 周辺)

```diff
   async function clearFailed() {
     const normalFailedCount = state.queue.filter((q) =>
       q?.status === 'failed' && q?.lastError !== 'unknown_remote_state'
     ).length;
     const unknownCount = state.queue.filter((q) =>
       q?.status === 'failed' && q?.lastError === 'unknown_remote_state'
     ).length;
     if (normalFailedCount === 0 && unknownCount > 0) {
       window.alert(
-        '通常の失敗項目はありません。\n' +
-        '「要確認」(' + unknownCount + ' 件) は個別に対応してください。'
+        '【⚠️ 通常の失敗項目はありません（0 件）】\n\n' +
+        '「失敗」と表示されている ' + unknownCount + ' 件は\n' +
+        '「要確認（Sheets に書込み済みの可能性あり）」項目です。\n' +
+        '安全のため一括削除はできません。\n\n' +
+        'Sheets を確認の上、各行の「🗑 削除」ボタンで個別に削除してください。'
       );
       return;
     }
```

---

## 5. retryFailed alert 文言改善（v1.4.8 拡張）

### options-queue.js — retryFailed の alert (line 438 周辺)

通常失敗 0 件のときも明示する:

```diff
       if (res.excludedUnknown && res.excludedUnknown > 0) {
-        const head = (res.retried && res.retried > 0)
-          ? 'リトライを開始しました（' + res.retried + ' 件）。\n'
-          : 'リトライ対象の通常失敗はありませんでした。\n';
-        window.alert(
-          head +
-          '⚠️ 「要確認」(' + res.excludedUnknown + ' 件) は対象外です。\n' +
-          'Sheets を確認の上、個別に「↻ 再送」ボタンから操作してください。'
-        );
+        const head = (res.retried && res.retried > 0)
+          ? '【リトライを開始しました（' + res.retried + ' 件）】\n\n'
+          : '【⚠️ リトライ対象の通常失敗はありません（0 件）】\n\n';
+        window.alert(
+          head +
+          '「要確認」(' + res.excludedUnknown + ' 件) は対象外です。\n' +
+          'Sheets に書込み済みの可能性があるため、自動再送できません。\n' +
+          'Sheets を確認の上、各行の「↻ 再送」ボタンで個別に操作してください。'
+        );
       }
```

---

## バージョン整合性

| ファイル | 変更 |
|---|---|
| `manifest.json` | "version": "1.4.8" → "1.4.9" |
| `popup.html` | "Version 1.4.8" → "Version 1.4.9" |
| `README.html` | "v1.4.8" → "v1.4.9" |

---

## 守るべき制約

- イミュータブル（map / filter / spread のみ）
- textContent only
- §17 (v1.4.8) / §16 (v1.4.7) / §15 (2026-05-13) などの過去履歴コメントは **絶対に書き換えない**
- 新規追加コメントは §18 (v1.4.9) で統一
- v1.4.7 / v1.4.8 の既存機能（マイグレーション、防御 clamp、reviveOrphanedSending failed 固定、失敗フィルター、payload rows.id 等）に回帰なし

---

## 成功条件

### 段落統一
- [ ] yahuoku / rakuten / yahooshopping / hardoff の description が 1 行（改行なし、` | ` 区切り）になる
- [ ] mercari / mercari_shop / rakuma / paypayfurima / amazon / ebay の挙動は変わらない

### スタットカード分割
- [ ] サイドパネルに「失敗 N」「要確認 M」の 2 枚のカードが並ぶ
- [ ] 失敗カードは通常 failed のみカウント
- [ ] 要確認カードは unknown_remote_state のみカウント

### クリアボタン disabled
- [ ] 通常失敗 0 件のときクリアボタンが disabled（グレーアウト）
- [ ] ツールチップで「通常失敗はありません」と表示

### アラート文言改善
- [ ] clearFailed: 通常 0 件 + unknown あり → 強化文言（⚠️ + 改行 + 詳細説明）
- [ ] retryFailed: retried 0 件のとき「リトライ対象の通常失敗はありません」明示

### バージョン整合性
- [ ] manifest.json / popup.html / README.html すべて v1.4.9

### 既存機能の回帰なし
- [ ] 送信キュー基本動作
- [ ] サイドパネル UI
- [ ] AI 翻訳
- [ ] v1.4.7 失敗フィルター（失敗カードクリック）
- [ ] v1.4.8 reviveOrphanedSending failed 固定 / payload rows.id / マイグレーション / 防御 clamp
- [ ] 過去履歴コメント §15-17 に変更なし
